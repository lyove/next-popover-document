import React, { useEffect } from "react";
// import Popover, { PlacementType, EmitType } from "next-popover";
import "./style.css";

/**
 * HomepageStart
 * @returns Component
 */
const HomepageStart = () => {
  useEffect(() => {
    //
  }, []);

  return <section className="start-wrap"></section>;
};

export default HomepageStart;
